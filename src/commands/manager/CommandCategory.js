/**
 * Get specific command into a category.
 * "CATEGORY_NAME": "COMMAND"
 *
 * @type {{GAME: string[], CLEAR: string[]}}
 */
category = {
    "GAME": [ "play" ],
    "CLEAR": [ "clear", "cls" ]
}

module.exports = category;